<div class = "order_details", id= "order_details">
	<div><%= link_to 'Back', 'javascript:;', {:id => 'close_details'}, :remote => true %></div>
	<h3>Add Item</h3>
	<%= render 'order_detail_info' %>

	<%= form_for @detail, :remote => true do |f| %>
		<%= render 'order_details/order_detail_form', :f => f %>
		<%= f.submit "Add to order", :id => 'save_order_detail' %>	
	<% end %>	
	<div><%= button_to 'Clear order', @detail, :method => :delete, :id => "delete_detail", :remote => true %></div>
</div>

